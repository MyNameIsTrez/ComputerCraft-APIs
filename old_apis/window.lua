-- https://computercraft.info/wiki/Window_(API)

-- Creates and returns a new window object, similar to a wrapped monitor. Refer to the term API for a list of functions attached to it.
-- create(table parentTerm, number x, number y, number width, number height [, boolean visible])
function create(parent_term, x, y, width, height, visible)
	local window_object = {}

	return window_object
end
